# Lambda uses its execution role in AWS; never place AWS keys here.
AWS_REGION=us-east-1
BEDROCK_REGION=us-east-1
BEDROCK_MODEL_ID=amazon.nova-micro-v1:0
ORCHESTRA_LENS_DEMO_TOKEN=replace-with-a-32-character-minimum-demo-token
