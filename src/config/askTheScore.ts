import {askTheScoreDemoConfig} from './askTheScore.demo';

/** Local ignored demo config. It contains an access token but never AWS credentials. */
export const askTheScoreEndpoint = askTheScoreDemoConfig.endpoint;
export const askTheScoreToken = askTheScoreDemoConfig.token;
