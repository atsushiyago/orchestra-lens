/** Copy to askTheScore.demo.ts and fill with the deployed demo endpoint and token. */
export const askTheScoreDemoConfig = {
  endpoint: 'https://replace-after-deployment.execute-api.us-east-1.amazonaws.com/ask-the-score',
  token: 'replace-with-a-local-demo-token',
} as const;
